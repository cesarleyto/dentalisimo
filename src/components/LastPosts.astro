---
import { getCollection } from 'astro:content';

// Obtenemos todos los posts y los ordenamos del más nuevo al más viejo
const allPosts = await getCollection('blog');
const latestPosts = allPosts
    .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
    .slice(0, 3); // Tomamos solo los 3 primeros
---

<section class="lastposts-section">
    <h2 class="title-verdeazul">Conoce cómo transformamos sonrisas</h2>
    
    <div class="posts-grid">
        {latestPosts.map((post) => (
            <article class="post-card">
                <a href={`/blog/${post.id}`} class="card-link">
                    <div class="img-wrapper">
                        <span class="categoria-tag">{post.data.category || 'Salud Dental'}</span>
                        <img src={post.data.heroImage || '/og-image2.jpg'} alt={post.data.title} class="hover-zoom">
                    </div>
                    <div class="post-info">
                        <h3>{post.data.title}</h3>
                        <p>{post.data.description}</p>
                        <span class="leer-mas">Leer más →</span>
                    </div>
                </a>
            </article>
        ))}
    </div>

    <div style="margin-top: 40px;">
        <a href="/blog" class="btn-cta">Ir al Blog Completo</a>
    </div>
</section>

<style>
    .lastposts-section {
        background-color: var(--blanco);
        padding: 80px 20px;
        text-align: center;
    }
    .title-verdeazul {
        color: var(--verdeazul);
        font-size: 2.8rem;
        font-weight: 900;
        margin-bottom: 50px;
    }
    .posts-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 30px;
        max-width: 1200px;
        margin: 0 auto;
        text-align: left;
    }
    
    .post-card {
        background: #fff;
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        transition: transform 0.3s ease;
        height: 100%;
    }
    .post-card:hover {
        transform: translateY(-5px);
    }
    
    /* El enlace envuelve toda la tarjeta */
    .card-link {
        text-decoration: none;
        color: inherit;
        display: block;
        height: 100%;
    }

    .img-wrapper {
        width: 100%;
        height: 200px;
        overflow: hidden;
        position: relative; /* Necesario para la posición absoluta de la categoría */
    }
    .img-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }
    .post-card:hover .img-wrapper img {
        transform: scale(1.05); /* Efecto zoom a la foto al pasar el mouse por la tarjeta */
    }

    /* Etiqueta flotante en la esquina superior izquierda */
    .categoria-tag {
        position: absolute;
        top: 15px;
        left: 15px;
        z-index: 10;
        display: inline-block;
        background: var(--gradiente);
        color: white;
        padding: 5px 14px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 700;
        box-shadow: 0 4px 10px rgba(0,0,0,0.2); /* Sombra para que resalte sobre la foto */
    }

    .post-info {
        padding: 25px;
    }
    .post-info h3 {
        color: var(--verdeazul);
        font-size: 1.3rem;
        margin-bottom: 10px;
        font-weight: 700;
        line-height: 1.3;
    }
    .post-info p {
        color: var(--negro);
        font-size: 0.95rem;
        margin-bottom: 20px;
        opacity: 0.8;
    }
    .leer-mas {
        color: var(--verdeazul-2);
        font-weight: 700;
        transition: color 0.3s;
    }
    .post-card:hover .leer-mas {
        color: var(--verdeazul);
    }

    @media (max-width: 992px) {
        .posts-grid { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 600px) {
        .posts-grid { grid-template-columns: 1fr; }
        .title-verdeazul { font-size: 2rem; }
    }
</style>